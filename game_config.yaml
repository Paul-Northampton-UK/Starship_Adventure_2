# game_config.yaml
# Initial game state settings

start_room_id: player_cabin
start_power_state: emergency # Options: offline, emergency, main_power
# Active game pack selection (planned for multi-pack hub). When implemented, the engine will load
# rooms/objects/responses from `packs/<active_pack>/` instead of root `data/`.
# Example: active_pack: starship_adventure
active_pack: starship_adventure
# Add other initial settings here later (e.g., starting inventory, flags)